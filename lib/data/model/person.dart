class Person{
  String? name;
  String? email;
  String? socketID;
  String? chatID;
  bool? isActive;
  bool? isEngaged;

  Person(this.name, this.email, this.socketID, this.chatID, this.isActive, this.isEngaged);
}